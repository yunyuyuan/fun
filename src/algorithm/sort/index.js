import React from 'react';
import {doMount, urlSearchMap} from "@/utils";

const Sort = (props)=>{
    const search = urlSearchMap();
    return (
        <div className='algorithm-sort'>
        </div>
    )
}

doMount(<Sort/>)
